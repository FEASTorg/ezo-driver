# Examples

- `arduino_smoke/`: minimal Arduino-framework compile smoke test for the C API and Arduino `TwoWire` adapter
- `arduino_read/`: Arduino-framework example using the thin C++ wrapper
- `linux_read.c`: host-side Linux adapter example

Focused examples belong here.

Examples should demonstrate clear integration patterns and remain intentionally small. They are supporting material, not the primary API design surface.
